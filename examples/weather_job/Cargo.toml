[package]
name = "weather_job"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
reqwest = { version = "0.12.5", features = ["json", "blocking"] }
serde_json = "1.0"
chrono = "0.4.38"
